export class Calculation {
  mean: string
  median: string
  mode: string

  constructor(mean: string, median: string, mode: string) {
    this.mean = mean;
    this.median = median;
    this.mode = mode;
  }
}
